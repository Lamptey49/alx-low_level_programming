#include <stdio.h>
#include <string.h>
/**
 * main - Entry point
 *
 * Return : Always (0)
 */
int main(void)
{
	char word[100];

	strcpy(word, "\"Programming is like building a multilingual puzzle");
	puts(word);

	return (0);
}
